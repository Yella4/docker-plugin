<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.jenkins.plugins</groupId>
        <artifactId>plugin</artifactId>
        <version>4.72</version> <!-- Or latest stable plugin parent version -->
        <relativePath/> <!-- Required for plugin BOM to work correctly -->
    </parent>

    <groupId>io.jenkins.plugins</groupId>
    <artifactId>docker-plugin-modern</artifactId>
    <version>1.0.0</version>
    <packaging>hpi</packaging>
    <name>Docker Plugin (Modern)</name>
    <url>https://github.com/your-org/your-plugin</url>

    <properties>
        <jenkins.version>2.387.2</jenkins.version>
        <java.level>11</java.level> <!-- or 17 -->
        <enforcer.skip>false</enforcer.skip>
    </properties>

    <dependencies>
        <!-- Jenkins Core -->
        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>ssh-slaves</artifactId>
        </dependency>

        <dependency>
            <groupId>org.jenkins-ci.plugins</groupId>
            <artifactId>token-macro</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Add more plugins as needed, managed by BOM -->
    </dependencies>

    <build>
        <plugins>
            <!-- Enforce Java compatibility -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.4.1</version>
                <executions>
                    <execution>
                        <id>enforce-java</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[11,)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <scm>
        <connection>scm:git:https://github.com/your-org/your-plugin.git</connection>
        <developerConnection>scm:git:git@github.com:your-org/your-plugin.git</developerConnection>
        <url>https://github.com/your-org/your-plugin</url>
        <tag>HEAD</tag>
    </scm>

    <developers>
        <developer>
            <id>your-id</id>
            <name>Your Name</name>
            <email>your@email.com</email>
        </developer>
    </developers>
</project>
